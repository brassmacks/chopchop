


const VeggieMonsters = function(difficulty=easy) {
  // populate with 5 imgs
const skins = [];

  const veggieMonster = function() {
    this.x = x;
    this.y = y;
    this.type = skins[Math.floor(Math.random())];
    
  };
  // refactor to take in difficulty
  let count;
  if (difficulty === easy) level = [
    speed = 20,
    warble = 30, count = 5
  ];

  const genMonster = function() {

    
  };
      this.speed = speed;
      // this.warble = warble;
      this.startPos = [STARTX, YRANGE * Math.random() + YRANGE[1]];
      this.bushel = [].concat(genMonsters(level));
      // the group that any of the current vegMons belong to 
    
  



};
